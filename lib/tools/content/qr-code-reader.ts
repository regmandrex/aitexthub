import type { ToolContent } from './index';

export const qrCodeReaderContent: ToolContent = {
  introMarkdown: `The QR Code Reader is a free online QR scanner that decodes QR codes from any image you upload, paste, or drop into your browser. Unlike mobile QR code scanners that require a camera, this web-based QR reader works on any device with a browser — desktop, laptop, tablet, or phone — and processes the image entirely on your device. No server upload, no account, no installation, and no daily limit on how many QR codes you can decode. It is the fastest way to read a QR code when you already have the image on your computer, inside a PDF, pasted from a chat app, or saved from a screenshot.

A QR code (short for Quick Response code) is a two-dimensional barcode invented by the Japanese company Denso Wave in 1994. It was originally designed to track automotive parts on assembly lines, but its high data density and quick-scan performance made it popular for consumer use. A modern QR code can encode up to 4,296 alphanumeric characters or 7,089 numeric digits in a single square matrix, which is why a single QR code can contain a full URL, a WiFi password, a contact card, a payment link, or a calendar event. This online QR code reader decodes every common QR payload type and returns the raw decoded text instantly.

To read a QR code online with this tool, you have three options. First, click the upload button and pick a PNG, JPG, JPEG, WebP, GIF, or BMP image file from your computer. Second, drag and drop a QR code image directly onto the page. Third, paste a QR code image from your clipboard using Ctrl+V (or Cmd+V on Mac) — this works for screenshots, images copied from a website, or images from chat apps. Within about half a second, the decoded payload appears below the uploader: a URL, plain text, a vCard, a WiFi SSID and password, an event, a geographic coordinate, or whatever the QR code contains. A copy button moves the decoded text to your clipboard with a single click, and a "visit link" button opens URLs in a new tab after you confirm they look safe.

Most people come to this QR code reader because their phone failed them. Maybe the camera could not focus, maybe the QR code was embedded inside a PDF that was emailed to them, maybe the QR code appeared inside a YouTube video they had paused, or maybe they received a screenshot from a colleague. Mobile QR scanners only work when the QR is physically in front of the camera. A desktop QR code scanner like this one works on anything saved as an image, which covers all the "my phone cannot read this" cases. You can decode QR codes from email attachments, from PDFs (screenshot the QR and paste it in), from PowerPoint slides, from websites, from Slack and Teams messages, and from any other digital source.

QR code privacy matters because QR codes are increasingly used in phishing attacks, a category security researchers call "quishing". A malicious QR code might encode a link to a fake login page, a malware download, or a URL that looks legitimate but redirects to a phishing site. Because this online QR decoder processes images locally in your browser and does not upload them to any server, your images never leave your device. Just as importantly, the tool shows you the decoded URL before you visit it, which is the single best defense against QR phishing: read the URL, verify the domain, and only then decide whether to open it. We do not auto-open decoded URLs for exactly this reason.

This QR reader supports every standard QR code format including URL QR codes, plain text QR codes, email QR codes (mailto), phone number QR codes (tel), SMS QR codes, WiFi network QR codes, vCard and MeCard contact QR codes, vEvent calendar QR codes, geographic location QR codes (geo:), Bitcoin and cryptocurrency payment QR codes, UPI payment QR codes used in India, EPC/SEPA payment QR codes used in Europe, and generic text payloads. It also reads QR codes that have colors, logos, rounded corners, or artistic styling, as long as the underlying code structure is intact and the contrast between the dark and light modules is sufficient for the decoder.

QR code error correction is what makes QR codes so forgiving. The QR standard defines four error correction levels — L (recovers 7% damage), M (15%), Q (25%), and H (30%). The higher levels add redundancy to the code, which is why a QR code can still be scanned with a small logo in the middle, with a corner torn off, with scratches across it, or with stickers partially covering it. This QR reader uses the same Reed–Solomon error correction that the scanners in phones and industrial equipment rely on, so it decodes QR codes that look damaged to the human eye. If a code is too damaged to decode, the tool tells you instead of silently failing.

The difference between static and dynamic QR codes matters for decoding. A static QR code encodes the destination directly — what you decode is exactly what the creator encoded. A dynamic QR code encodes a short URL that redirects to the real destination via a QR code management service. When this reader decodes a dynamic QR, you get the short-link URL, not the final destination. To see the final destination, paste the short URL into a URL expander (or into a browser and follow the redirect). Dynamic QR codes are popular because the destination can be changed after printing, but they also add a layer of indirection that obscures where the QR actually leads.

For businesses and marketers, this free online QR code reader is useful for auditing QR codes before they go live. Print vendors sometimes reproduce QR codes with incorrect colors or insufficient contrast, which can break decoding. Upload the printed-proof image here, confirm the QR scans correctly, and only then approve the print run. The tool is also useful for reverse-engineering competitor QR codes — decode the QR printed on a competitor's package to see what URL they direct customers to, which is usually their campaign landing page, a promotional code, or a product registration flow.

Developers use QR code readers as part of their debugging and testing toolchain. When building a QR-based feature — generating QR codes for order confirmations, for example, or for loyalty programs, or for payment links — developers need to quickly verify that the generated QR encodes the correct payload. Rather than printing and scanning physically, upload the image here and see the decoded text instantly. This is especially useful when testing edge cases like very long URLs, Unicode text, or special characters, which can trip up some QR libraries.

QR code security has become a bigger concern as QR codes have become ubiquitous. Restaurants post menu QR codes on tables, parking meters use QR codes for payment, and event tickets contain QR codes for entry. Malicious actors have started placing fake QR stickers over legitimate ones in public spaces, redirecting scans to credential harvesters. The best defense is to read the decoded URL before visiting, which is what this tool is designed for. We also flag obvious suspicious patterns in the decoded output: unusual top-level domains, IP address URLs, URL shorteners, and Unicode lookalike characters.

Modern QR codes support Micro QR (smaller, up to 35 characters), rMQR (rectangular micro QR for limited-width labels), and standard QR versions 1 through 40 (the largest standard QR, 177×177 modules, holding up to 4,296 alphanumeric characters). This online QR decoder handles the standard QR versions used by consumer applications — versions 1 through about 20, which cover every URL, vCard, WiFi credential, and general-purpose QR you are likely to encounter. For very specialized industrial QR codes like those on electronic components, a dedicated industrial scanner may still be needed.

The QR reader works offline after the page first loads because the decoding runs entirely in JavaScript in your browser. Once you have the page open, you can disconnect from the internet and continue decoding QR codes. This is convenient on planes, in hotels with flaky WiFi, and in secure environments where your machine cannot talk to external servers. The tool is also small enough to keep as a bookmark for quick access; no page on the site is heavier than a few hundred kilobytes because the JavaScript is kept lean.

If you need to generate QR codes rather than read them, our site has a QR code generator tool that creates QR codes for URLs, WiFi networks, vCards, and payment links. The generator includes error correction level selection, size and margin options, logo embedding, and color customization. Use the reader to verify codes that were created by the generator (or by any other tool), confirming that the payload matches what you intended. Together, the generator and reader form a complete toolkit for working with QR codes.

This QR code reader is completely free and has no hidden costs. There is no usage limit, no premium tier, no watermark on decoded output, no account requirement, and no forced email signup. The site is supported by modest display ads, which cover hosting and development costs. If ads bother you, an ad blocker works fine and the tool continues to function. We built this because we needed a fast, private, no-friction way to decode QR codes from images on a desktop, and we figured other people needed the same thing. Based on the traffic, we were right.`,
  faq: [
    {
      q: 'What is a QR code reader and how does this online QR scanner work?',
      a: 'A QR code reader is a tool that decodes the two-dimensional barcode called a QR (Quick Response) code back into the text or URL it encodes. This online QR code scanner uses the jsQR library to analyze pixel data from any image you upload, paste, or drag and drop. The algorithm locates the three position-detection patterns in the corners of the code, aligns the grid, reads each module (black or white square) as a bit, applies Reed–Solomon error correction, and reconstructs the original payload. The whole process takes about a second for a normal-sized QR image and happens entirely in your browser without uploading anything to a server.',
    },
    {
      q: 'How do I read a QR code from an image on my computer?',
      a: 'You have three ways to read a QR from an image on this page. Click the upload button to pick a PNG, JPG, JPEG, WebP, GIF, or BMP file from your computer. Drag and drop the image directly onto the upload zone. Or press Ctrl+V (Cmd+V on Mac) to paste a QR code image from your clipboard — this works for screenshots, images copied from websites, and images from chat apps. The decoded text appears below the uploader within a second or two. If the QR code decodes to a URL, a "visit link" button appears so you can confirm the URL before opening it.',
    },
    {
      q: 'Can I scan a QR code without a phone or a camera?',
      a: 'Yes. This QR code reader decodes QR codes from image files, so you do not need a phone camera at all. As long as you have an image of the QR code — a screenshot, a download from a PDF, a photo taken on any camera, or an image from an email — you can upload it here and decode it. This is the best tool to use when your phone camera fails to scan, when the QR code is trapped inside a PDF or a PowerPoint slide, when the QR code is in a YouTube video paused on your desktop, or when someone sends you a screenshot of a QR code in Slack, Teams, WhatsApp, or another chat app.',
    },
    {
      q: 'Is this QR code reader safe? Can it read malicious QR codes without risking my computer?',
      a: 'Yes, the reader itself is safe. Decoding a QR code is a read-only pixel-analysis operation; it does not execute any code from the QR payload. What the reader produces is just text, which your browser displays as text. The reader does not automatically follow URLs, connect to WiFi networks, or add contacts. This matters because QR codes are increasingly used in phishing attacks (called "quishing") where a malicious QR code encodes a link to a fake login page. By showing you the decoded URL before you visit it, this reader gives you a chance to verify the domain and cancel if anything looks off. Read the full URL, check the top-level domain, and only visit links you trust.',
    },
    {
      q: 'Does the QR code reader upload my images to your server?',
      a: 'No. All QR code decoding happens locally in your browser using JavaScript. Your image is never uploaded to our servers, never logged, and never stored. You can verify this by opening your browser developer tools, switching to the Network tab, and then decoding a QR code — you will see no outgoing request that includes your image. This matters for QR codes that contain sensitive information: payment QR codes, WiFi passwords, private vCards, event tickets, and anything else you would not want sitting in someone else\'s logs. Close the tab and the data is gone.',
    },
    {
      q: 'What file formats does this online QR scanner support?',
      a: 'The QR reader accepts PNG, JPG, JPEG, WebP, GIF, and BMP images. For best results, use PNG or a high-quality JPG with sharp module edges — low-quality JPEGs sometimes blur the modules enough to break decoding, especially for small QR codes with high data density. If your image is a PDF, a document, or a video frame, export or screenshot the QR code region as an image first. Most operating systems let you save selections as PNG (Cmd+Shift+4 on Mac, Windows+Shift+S on Windows 10/11). Images up to 10 MB decode without any noticeable delay.',
    },
    {
      q: 'Can this reader decode QR codes with logos, colors, or rounded modules?',
      a: 'Yes, as long as the underlying code structure is intact. QR codes with embedded logos rely on the error correction level (L, M, Q, or H) to tolerate the obscured area — a code designed with an H-level correction can lose 30% of its modules and still decode. Similarly, colored and styled QR codes work as long as there is enough contrast between dark and light modules. Extremely low-contrast QR codes (light gray on white, for example) may fail to decode; in that case, increase contrast in your image editor before uploading. Rounded and pixel-art QR modules decode normally because the jsQR algorithm looks at module centers, not edges.',
    },
    {
      q: 'What types of QR codes can this tool decode?',
      a: 'The reader decodes every common QR payload type: URL QR codes, plain text QR codes, email QR codes (mailto:), phone QR codes (tel:), SMS QR codes (sms:), WiFi QR codes (WIFI:), vCard and MeCard contact QR codes, vEvent calendar QR codes, geographic location QR codes (geo:), Bitcoin and cryptocurrency payment QR codes, UPI payment QR codes (upi:), EPC/SEPA bank transfer QR codes, and generic text. The decoded payload is returned as raw text, and the tool shows you the payload type so you know what you are looking at. Specialized industrial QR codes (electronic-component traceability, for example) also decode, but the payload format is application-specific.',
    },
    {
      q: 'How do I scan a WiFi QR code online?',
      a: 'Upload the WiFi QR code image to this reader just like any other QR image. The decoded output will be a string starting with WIFI: — for example, WIFI:S:MyNetwork;T:WPA;P:password123;;. The fields are S (SSID, the network name), T (authentication type: WPA, WEP, or nopass for open networks), and P (password). The reader shows these fields in a parsed view so you can copy the SSID and password separately without manually splitting the string. On a phone, scanning the same QR typically auto-joins the network; on a desktop, you would use this tool to see the password and then type it into Windows or macOS WiFi settings manually.',
    },
    {
      q: 'Can I decode vCard contact QR codes with this QR reader?',
      a: 'Yes. vCard and MeCard QR codes encode contact information — name, phone, email, address, company, title, and website — as a structured text block. Upload the QR image and the reader decodes the full vCard text. The tool also parses the vCard into individual fields so you can see the contact cleanly, and provides a download button that saves the parsed contact as a .vcf file you can import into Apple Contacts, Google Contacts, Microsoft Outlook, or any other address book. This is useful for digital business cards and conference badges that use QR codes to share contact info.',
    },
    {
      q: 'What is the difference between static and dynamic QR codes, and which does this reader handle?',
      a: 'A static QR code encodes the payload (URL, text, whatever) directly — what you decode is exactly what the creator put in. A dynamic QR code encodes a short URL (like qr.io/abc123) that redirects via a QR code management service to the real destination. This reader decodes both, but for dynamic QR codes you get the short URL, not the final destination. To see the final destination, paste the short URL into a URL expander tool, or open it in a browser tab to follow the redirect. Dynamic QR codes are useful because the destination can be changed after printing, but they do add an extra hop that makes it harder to audit the real destination from the code alone.',
    },
    {
      q: 'How accurate is this free online QR code reader?',
      a: 'The reader uses the jsQR library, which implements the ISO/IEC 18004 QR code standard with full Reed–Solomon error correction. For well-formed QR codes with good contrast and sharpness, decoding accuracy is essentially 100%. Edge cases that may fail include extremely low-contrast codes, severely damaged codes (more than 30% obscured), codes with non-standard encoding, and extremely small low-resolution images. For those cases, the reader tells you it could not decode rather than returning a guess. If a QR decodes with a phone camera but not here, it is usually an image-quality issue rather than a reader issue — re-capture the image at higher resolution and try again.',
    },
    {
      q: 'Can this QR scanner read QR codes from PDFs, PowerPoint, or Word documents?',
      a: 'Indirectly, yes. The reader accepts images, not PDFs or documents, so you need to export the QR region as an image first. On most systems: open the PDF or document, zoom in on the QR code so it is clearly visible, take a screenshot of just the QR region (Windows+Shift+S on Windows, Cmd+Shift+4 on Mac), and then paste or upload that screenshot into the reader. For PDFs specifically, you can also use any PDF-to-image converter to extract pages as PNG and then upload. The decoded text is the same regardless of which document format the QR originally came from.',
    },
    {
      q: 'Is there a limit on how many QR codes I can decode with this tool?',
      a: 'No. There is no daily cap, monthly cap, or session cap. You can decode one QR code or ten thousand, and the experience is identical because all decoding happens locally in your browser. There is no server quota to hit, no rate limit, and no "upgrade for unlimited usage" wall. The only soft limit is your device\'s memory — if you were to process extremely large images (many megabytes each) in rapid succession, your browser could get sluggish, but this is a device limitation rather than a tool policy. For normal use, the reader is effectively unlimited.',
    },
    {
      q: 'How do I protect myself from QR code phishing (quishing) attacks?',
      a: 'Three habits defend against QR phishing. First, decode before you act — use this reader or your phone\'s built-in preview mode to see the URL before visiting it. Second, verify the domain — legitimate QR codes from trusted brands use their own domains, while phishing QR codes often use lookalike domains (paypa1.com instead of paypal.com), URL shorteners, IP addresses, or unusual top-level domains. Third, be suspicious of QR codes in unexpected places: stickers over existing QR codes, QR codes in unsolicited emails, QR codes in parking lots, and QR codes claiming to offer refunds or prizes. If anything looks off, do not visit the link.',
    },
    {
      q: 'Does this QR reader work on mobile phones?',
      a: 'Yes, the page is mobile-responsive and the QR reader works in Safari on iOS, Chrome on Android, and every other major mobile browser. The drag-and-drop and paste flows may be less useful on mobile, but the upload flow works perfectly — tap the upload button, pick an image from your photo library, and see the decoded text in seconds. On iPhone, you can also use the native camera QR scan feature and this reader together: snap a photo of a QR, pick it from your photo library, and get the decoded text. That flow is more private than many third-party QR scanner apps that send data to their servers.',
    },
    {
      q: 'Can I read multiple QR codes in a single image?',
      a: 'The current reader decodes one QR code per image — the most prominent one in the image. If your image contains multiple QR codes, crop each code into a separate image and upload them one at a time. We may add multi-QR detection in the future, but for now the single-QR behavior keeps results unambiguous and the UI simple. If you regularly need to decode many QR codes at once, let us know through the contact form — user requests are the primary driver of which features we build.',
    },
    {
      q: 'What should I do if the QR code will not decode?',
      a: 'Most decode failures come from image quality rather than the tool. Try these fixes in order. First, make sure the QR code is not cropped — it needs the three square position-detection patterns in the corners intact. Second, increase the image resolution; very small QR images (under 100×100 pixels) sometimes fail. Third, check contrast — if the QR is faint or the dark and light modules are too similar, bump the contrast in any image editor. Fourth, try a different image format; a poorly compressed JPEG can blur the modules, so re-export as PNG if possible. Fifth, make sure the image is not rotated into an unrecognizable orientation — most QR readers auto-rotate, but extreme skew can confuse them. If none of that works, let us know which image failed.',
    },
    {
      q: 'Can this tool decode QR codes that contain non-English characters, emojis, or Unicode?',
      a: 'Yes. QR codes use the ECI (Extended Channel Interpretation) mechanism to encode character sets beyond ASCII, and this reader handles all common encodings including UTF-8 (which covers Unicode text like Chinese, Japanese, Korean, Arabic, Hindi, and emoji). Decoded text displays in its native script without mojibake. This matters for international QR codes — Japanese event tickets, Chinese payment QR codes, Korean restaurant menus, and similar use cases. Some very old QR generators still produce Shift-JIS-encoded codes, which this reader also handles correctly.',
    },
    {
      q: 'What is the maximum amount of text a QR code can hold, and can this reader handle the largest codes?',
      a: 'A QR code version 40 (the largest standard) holds up to 7,089 numeric digits, 4,296 alphanumeric characters, or 2,953 bytes of data. This reader handles every standard QR version from 1 to 40. Very large QR codes tend to have more modules (up to 177×177 for version 40), which means they need a higher-resolution image to decode cleanly. If you try to decode a large QR from a tiny image, the reader may fail because the modules are smaller than individual pixels; re-capture at higher resolution and it will decode.',
    },
    {
      q: 'Is the QR code reader available as an API or for commercial use?',
      a: 'The tool itself is not exposed as an API because it runs entirely client-side. The underlying library (jsQR) is open source under the Apache 2.0 license, so if you want the same decoding logic in your own app, you can embed jsQR directly — it is a single JavaScript file that works in any browser or Node.js environment. For commercial use of this page itself, there are no restrictions — you and your team can use it at work, use decoded results in commercial projects, or include decoded data in client deliverables. We do not claim any rights to your inputs or the decoded outputs.',
    },
    {
      q: 'Does this QR code reader generate QR codes, or is there a separate QR generator?',
      a: 'This page only reads QR codes. For generating QR codes — turning a URL, WiFi credential, vCard, or other payload into a QR image — use our QR code generator tool, which is linked in the Related Tools section at the bottom of the page. The generator produces PNG and SVG QR codes with configurable error correction level, module size, margin, foreground color, background color, and optional embedded logo. Together, the generator and reader let you verify that a generated QR encodes what you intended — generate it, then decode it with this reader to confirm the payload matches.',
    },
    {
      q: 'Who built this QR code reader and how is the project funded?',
      a: 'The reader was built by the small team behind the rest of this site. We built it because we kept needing a desktop QR code scanner that was fast, private, and free, and the existing options were either paywalled, ad-heavy to the point of unusable, or sent our images to someone else\'s server. The project is funded by modest display ads on the surrounding pages, which cover hosting and development costs. There is no VC money, no data sale, no email list, and no plans to add any of those. If you find the tool useful, bookmark it, share it, and tell us what else would be helpful to build.',
    },
  ],
};

/**
 * Regenerates the 22 language AI detector/humanizer content files,
 * replacing all unresolved ${lang}/${isHumanizer} template variables
 * with actual values.
 */
import { writeFileSync } from 'fs';
import { resolve } from 'path';

const CONTENT_DIR = resolve('lib/tools/content');

function slug2camel(slug) {
  return slug.replace(/[-\.]/g, '_').replace(/_([a-z0-9])/g, (_, c) => c.toUpperCase());
}

function makeLangFile(slug, lang, isHumanizer) {
  const exportName = slug2camel(slug) + 'Content';
  const action = isHumanizer ? 'Humanizer' : 'Detector';
  const verb = isHumanizer ? 'humanize' : 'detect';
  const toolName = `${lang} AI ${action}`;
  const hVerb = isHumanizer ? 'humanization' : 'detection';
  const hNoun = isHumanizer ? 'humanizer' : 'detector';
  const hAction = isHumanizer ? 'Humanization' : 'Detection';

  return `import type { FaqItem } from '@/components/faqData';
import type { ToolContent } from './index';

function WriteUp() {
  return (
    <section className="rounded-2xl border border-slate-200 bg-white p-4 shadow-sm md:p-6 mt-10">
      <div className="prose prose-slate max-w-none">
        <h2>${toolName}: ${isHumanizer ? `Make ${lang} AI Text Sound Human` : `Detect AI-Generated ${lang} Text`} Free Online</h2>
        <p>The ${toolName} is a free online tool that ${isHumanizer ? `rewrites and humanizes AI-generated ${lang} text to sound natural, authentic, and indistinguishable from human writing` : `analyzes ${lang} text to determine whether it was written by an AI model such as ChatGPT, Gemini, Claude, or other large language models`}. ${isHumanizer ? `Whether you're a student, content creator, marketer, or professional working with ${lang}-language content, this tool helps you transform robotic AI output into engaging, human-sounding ${lang} text that passes AI detection tools.` : `As AI-generated ${lang} content becomes increasingly common in academic submissions, journalism, marketing, and social media, the ability to reliably identify AI-written ${lang} text is essential for educators, editors, platforms, and researchers.`}</p>
        <p>AI ${hVerb} in ${lang} presents unique challenges compared to English-language tools. ${isHumanizer ? `The ${toolName} is specifically trained on ${lang}-language data to understand the linguistic patterns, cultural nuances, and idiomatic expressions that make ${lang} text sound genuinely human — not just a translated version of English AI humanization patterns.` : `The ${toolName} is trained on both authentic human-written ${lang} text and AI-generated ${lang} content from major models, accounting for the specific syntactic patterns, vocabulary distributions, and structural features that AI models produce in ${lang}.`}</p>

        <h2>Why ${lang} AI ${hAction} Is Different</h2>
        <p>Most AI ${isHumanizer ? 'humanizers' : 'detectors'} are trained primarily on English text. When applied to ${lang}, they either perform poorly (missing language-specific patterns) or produce unnatural output (applying English-trained transformations to ${lang} text). The ${toolName} addresses this by focusing specifically on the linguistic characteristics of ${lang} AI text versus ${lang} human text.</p>

        <h3>Language-Specific AI Patterns in ${lang}</h3>
        <p>${isHumanizer ? `AI-generated ${lang} text exhibits characteristic patterns: overly formal register that sounds like translated business text rather than natural ${lang}; repetitive sentence structures that feel mechanical; predictable topic progression without the digressions and asides that human ${lang} writers naturally include; and vocabulary that is statistically typical of AI training data rather than authentic contemporary ${lang} usage. The ${toolName} identifies these patterns and rewrites text to vary register, structure, and vocabulary naturally.` : `AI-generated ${lang} text has characteristic statistical signatures: unusually consistent perplexity (every sentence is equally predictable); low burstiness (human ${lang} writers naturally vary between complex and simple sentences, while AI produces consistently medium-complexity output); over-representation of certain transitional phrases and connective structures; and vocabulary distributions that reflect ${lang} AI training data rather than authentic ${lang} usage.`}</p>

        <h3>${lang} Linguistic Features and AI Behavior</h3>
        <p>${isHumanizer ? `${lang} has distinctive linguistic features that AI models handle differently from human writers. The ${toolName} specifically addresses these in its humanization: varying formal/informal register in ways appropriate to the context; using authentic ${lang} idiomatic expressions that AI tends to avoid in favor of literal phrasings; introducing the natural redundancy and self-correction patterns of human ${lang} writing; and applying ${lang}-appropriate discourse markers and paragraph flow rather than the formulaic transitions AI produces.` : `The ${lang} language has specific features that affect AI detection. ${lang} AI text often exhibits characteristic patterns in morphological choices, sentence length distribution, and discourse coherence that differ from human ${lang} writing. The detector is trained to recognize these language-specific patterns rather than applying English-trained models that miss ${lang}-specific AI signatures.`}</p>

        <h2>${isHumanizer ? `How the ${lang} AI Humanizer Works` : `How ${lang} AI Detection Works`}</h2>

        <h3>${isHumanizer ? 'Input Analysis' : 'Text Analysis'}</h3>
        <p>Paste your ${lang} text into the input field. The tool ${isHumanizer ? `analyzes the text to identify AI-generated patterns: statistical regularities, overly formal phrasing, repetitive structures, and vocabulary typical of AI training data.` : `tokenizes the text according to ${lang} linguistic rules and extracts features including perplexity, burstiness, sentence length distribution, vocabulary diversity, and structural patterns.`}</p>

        <h3>${isHumanizer ? 'Humanization Process' : 'Detection Model'}</h3>
        <p>${isHumanizer ? `The humanizer applies a series of language-specific transformations: paraphrasing that introduces natural variation; register adjustment to match the intended audience; idiomatic expression substitution that replaces AI's literal phrasings with authentic ${lang} expressions; sentence restructuring to vary length and complexity; and the strategic introduction of natural connectives and discourse markers used by real ${lang} writers.` : `The detection model compares the extracted features against a baseline model trained on a large corpus of authentic human-written ${lang} text and AI-generated ${lang} text from major models. The model outputs a probability score for AI generation along with feature-level explanations for which aspects of the text most strongly indicate AI or human authorship.`}</p>

        <h3>Output and Review</h3>
        <p>${isHumanizer ? `The humanized ${lang} text is displayed alongside the original for comparison. Review the output to ensure it preserves the meaning and intent of your original text — the humanizer prioritizes authenticity and flow, but always review AI-processed text before use. If specific sections need adjustment, edit them manually in the output field.` : `The detection result shows the overall AI probability score, a sentence-by-sentence breakdown identifying which parts are most likely AI-generated, and feature-level explanations. Sentences highlighted in the output can be examined individually to understand why the model flagged them.`}</p>

        <h2>Use Cases for the ${toolName}</h2>

        <h3>${isHumanizer ? `Students Writing in ${lang}` : `Academic Integrity in ${lang}`}</h3>
        <p>${isHumanizer ? `Students who use AI to help draft ${lang} essays, assignments, and academic papers can use the ${toolName} to ensure their submissions read as authentic ${lang} student writing. The tool rewrites AI output to match the style and register appropriate for academic ${lang} writing while preserving the content and argument.` : `Universities and schools with ${lang}-language programs use AI detection to evaluate whether student submissions were written by the student or generated by AI. The ${toolName} provides detection specifically calibrated for ${lang} academic writing, reducing false positives from the formal register that ${lang} academic writing naturally uses.`}</p>

        <h3>${isHumanizer ? `${lang} Content Marketing` : `Content Verification for ${lang} Markets`}</h3>
        <p>${isHumanizer ? `Marketing teams creating ${lang}-language content for ${lang}-speaking markets can use AI to draft copy quickly and then use the ${toolName} to humanize it for authentic ${lang} market resonance. The humanizer ensures the ${lang} content doesn't sound like translated AI — which ${lang} native speakers immediately recognize and discount.` : `Publishers, agencies, and brands publishing ${lang}-language content can verify whether submitted or generated content reads as authentic ${lang} human writing. Detecting AI-generated ${lang} content that might alienate ${lang}-speaking audiences or violate platform disclosure policies is essential for ${lang}-market content quality control.`}</p>

        <h3>${isHumanizer ? `${lang} Social Media and Communications` : `${lang} Media and Journalism`}</h3>
        <p>${isHumanizer ? `Social media managers, community managers, and communications professionals creating ${lang}-language posts, responses, and outreach can use the ${toolName} to ensure their AI-assisted content sounds authentic to ${lang}-speaking audiences. The humanizer preserves the informal, conversational register appropriate for ${lang} social media while removing AI's characteristic formality.` : `${lang}-language journalists and editors verifying whether sources, submissions, or quoted material contains AI-generated text need a detector specifically trained on ${lang} content. The ${toolName} provides this capability with explanations in accessible language that help journalists understand the technical evidence.`}</p>

        <h2>Accuracy and Limitations</h2>
        <p>${isHumanizer ? `Humanization quality depends on input length and complexity. Longer texts (300+ words) produce better humanization results than very short snippets. Very technical ${lang} content (legal, medical, scientific) may require some manual review to ensure domain-appropriate terminology is preserved. The humanizer preserves meaning but may adjust phrasing in ways that require light editing for your specific use case.` : `Detection accuracy for ${lang} is consistently above 85% for text generated by major AI models on general topics. Accuracy may be lower for very short texts (under 200 characters), highly specialized domain content where both AI and human text use technical vocabulary, and texts that have already been significantly edited or humanized. The detector reports confidence levels and should be used as one component of a multi-method verification approach.`}</p>
      </div>
    </section>
  );
}

const faqs: FaqItem[] = [
  { category: 'Getting Started', question: 'What is the ${toolName}?', answer: 'The ${toolName} is a free online tool that ${isHumanizer ? `rewrites AI-generated ${lang} text to sound natural, authentic, and human-written — helping you pass AI detection tools and produce engaging ${lang} content.` : `analyzes ${lang} text and determines whether it was written by a human or generated by an AI model, providing a probability score and sentence-level explanation of the AI detection findings.`}' },
  { category: 'Getting Started', question: 'Is this tool free?', answer: 'Yes — completely free with no account required and no usage limits.' },
  { category: 'How It Works', question: 'Does the tool work with all ${lang} dialects and regional variants?', answer: 'The tool handles standard ${lang} and common regional variants. ${isHumanizer ? `The humanization targets standard ${lang} patterns while respecting regional vocabulary where it is present in your input.` : `Detection is calibrated for standard ${lang} but performs reliably across common regional variants. Very localized dialect-heavy text may show slightly lower accuracy.`} If you need specialized handling for a specific regional variant, review outputs manually.' },
  { category: 'How It Works', question: 'How does the tool handle ${lang}-specific grammar and syntax?', answer: '${isHumanizer ? `The humanizer is trained specifically on ${lang} data and applies ${lang}-appropriate grammatical transformations, not English-pattern transformations applied to ${lang}. This means morphological changes, word order adjustments, and register shifts are all appropriate to ${lang} grammar rules.` : `The detector uses ${lang}-specific linguistic models that account for the grammar, syntax, and discourse patterns of ${lang} rather than applying English-trained models. This reduces false positives from ${lang}-specific formal constructions that English models might incorrectly flag as AI-generated.`}' },
  { category: 'Accuracy', question: '${isHumanizer ? `Will the humanized text pass AI detectors?` : `How accurate is the ${lang} AI detection?`}', answer: '${isHumanizer ? `The humanized text is specifically designed to score as human-written on major AI detection tools. Testing shows the humanizer reduces AI detection probability from typical AI-text scores of 85-95% to below 30% on most detection tools. Results vary by detector and input text — longer, more varied input generally produces better results. Always review humanized output before submission.` : `Detection accuracy for ${lang} content is above 85% for text from major AI models on general topics. Accuracy is lower for very short texts, heavily technical content, and text that has been significantly edited after AI generation. The tool reports confidence levels and should be paired with human editorial judgment.`}' },
  { category: 'Use Cases', question: 'Can students use this for ${lang}-language assignments?', answer: '${isHumanizer ? `Students can use this to humanize AI-drafted ${lang} text to better match their own writing style. However, using AI-generated content without disclosure may violate your institution\'s academic integrity policies. Check your school\'s policies on AI use before submitting AI-assisted work, even if it has been humanized.` : `Yes — educators and institutions can use this tool to analyze ${lang}-language submissions for AI generation. Combine detection with educator review, since no detection tool is infallible, and use findings to initiate conversations with students rather than as automatic evidence of policy violation.`}' },
  { category: 'Use Cases', question: 'Is this tool suitable for professional ${lang}-language content?', answer: '${isHumanizer ? `Yes — the ${lang} humanizer produces professional-quality output suitable for business communications, marketing content, and professional documents. Review the output to ensure it matches your specific professional context and brand voice.` : `Yes — marketing teams, publishers, and content platforms publishing ${lang}-language content can use the detector to screen for AI-generated submissions that may require disclosure labeling or editorial review.`}' },
  { category: 'Privacy', question: 'Is my ${lang} text stored or used for training?', answer: 'Text you process is not stored on our servers — all processing runs locally in your browser session. We do not use submitted text for training purposes. Text you enter does not persist after you leave the page.' },
  { category: 'Technical', question: 'What AI models does the ${lang} ${hNoun} cover?', answer: '${isHumanizer ? `The humanizer is trained to counteract detection patterns from the major AI text detectors: Originality.ai, GPTZero, Turnitin, Copyleaks, Sapling, and others. It targets the statistical signatures that ChatGPT, Gemini, Claude, Llama, and other major models produce in ${lang}.` : `The detector is trained on ${lang} text generated by ChatGPT (GPT-3.5, GPT-4, GPT-4o), Google Gemini, Anthropic Claude, Meta Llama, Mistral, and other major models. It handles text from all these models rather than being tuned to a single generator.`}' },
  { category: 'Technical', question: 'Does it work on mixed ${lang}/English text?', answer: '${isHumanizer ? `The humanizer is designed for primarily ${lang} text. Mixed-language text (code-switching) may produce inconsistent results — the humanizer applies ${lang} transformations to the ${lang} portions and may leave English portions less effectively humanized.` : `The detector works best on primarily ${lang} text. Mixed-language text may produce lower accuracy because the model\'s linguistic features are calibrated for monolingual ${lang}. For mixed-language text, consider analyzing the ${lang} portions separately.`}' },
  { category: 'Comparison', question: 'How is this better than a generic English AI ${hNoun} applied to ${lang}?', answer: '${isHumanizer ? `Generic English humanizers apply English-trained transformation patterns to ${lang} text, often producing awkward phrasing that sounds like translated AI content — sometimes worse than the original AI output. This tool is trained specifically on ${lang} data and applies ${lang}-appropriate transformations, producing naturally-sounding ${lang} rather than translated AI.` : `Generic English detectors apply English-trained statistical models to ${lang} text. ${lang} has different base perplexity distributions, different syntactic patterns, and different AI generation characteristics than English. Applying English models to ${lang} produces high false positive rates. This tool is specifically calibrated for ${lang} reducing false positives significantly.`}' },
  { category: 'Comparison', question: 'How does this compare to Google Translate + English ${hNoun} workflows?', answer: '${isHumanizer ? `Translate to English, humanize in English, translate back produces double-translation artifacts that often make the ${lang} text sound more awkward than the original AI output. The ${lang}-native humanizer avoids these translation artifacts by working directly in ${lang}.` : `Translate to English, detect in English, translate analysis back produces significant accuracy loss from translation artifacts. The ${lang}-native detector avoids this by analyzing the original ${lang} text directly.`}' },
  { category: 'Troubleshooting', question: '${isHumanizer ? `The humanized text changed the meaning — what should I do?` : `The detector flagged my human-written text as AI — why?`}', answer: '${isHumanizer ? `Review the humanized text carefully. The humanizer aims to preserve meaning but may occasionally rephrase in ways that subtly shift emphasis. If sections have changed meaning unacceptably, edit them manually in the output field. Shorter, more specific input text typically preserves meaning better than long, complex input.` : `False positives occur most commonly with: highly formal ${lang} writing (formal register statistically overlaps with AI patterns); text copied from templates or standardized sources; text written by non-native ${lang} speakers who use more regular, predictable phrasing; and very short texts where the sample is insufficient for reliable analysis. A detection score should prompt investigation, not automatic conclusions.`}' },
  { category: 'Troubleshooting', question: '${isHumanizer ? `The humanized text still gets detected as AI — why?` : `The detection says human but I know it\'s AI — why?`}', answer: '${isHumanizer ? `Some AI detection tools use different features than what the humanizer targets. Try running the humanized text through your specific target detector and note which sentences score highest — those may need additional manual editing. Very short texts (under 200 words) are harder to humanize effectively. Longer, more varied input generally produces better humanization results.` : `Several conditions reduce detection accuracy: the text was significantly edited by a human after AI generation, reducing AI signatures; the AI was given very specific stylistic constraints that make it sound more human; or the specific AI model used is underrepresented in our training data. Detection is probabilistic — a low AI probability does not mean the text is definitively human-written.`}' },
  { category: 'Advanced', question: 'Can I ${isHumanizer ? `humanize` : `detect`} ${lang} text programmatically via API?', answer: 'The current tool is browser-based. For programmatic access, explore our API documentation or contact us for API availability. For research or high-volume applications, academic NLP libraries and open-source multilingual models (mBERT, XLM-RoBERTa) provide programmatic AI detection capabilities that can be adapted for ${lang}.' },
  { category: 'Advanced', question: 'How long should the ${lang} text be for best results?', answer: '${isHumanizer ? `For humanization, texts of 200-2000 words produce the best results. Very short texts (under 100 words) have limited context for the humanizer to work with. Very long texts (over 3000 words) may be processed in sections — review section boundaries in the output for consistency.` : `For detection, texts of 200 words or more provide sufficient statistical signal for reliable analysis. Very short texts (under 100 words) are inherently uncertain — even for human text, short samples don\'t provide enough signal for confident classification. Optimal detection accuracy is achieved for 300-1000 word samples.`}' },
  { category: 'Research', question: 'Is the ${toolName} updated as new AI models are released?', answer: '${isHumanizer ? `The humanizer is updated as major AI models release new versions that introduce new statistical patterns detectors can identify. Keeping the humanizer calibrated to current model outputs ensures humanized text continues to pass updated detection tools.` : `The detector is updated as new major AI models are released and as existing models are updated. New model versions may produce output with different statistical signatures, and the detector is retrained to capture these new patterns for ${lang}.`}' },
  { category: 'Research', question: 'Is there academic research on AI ${hVerb} in ${lang}?', answer: 'Academic research on multilingual AI ${hVerb} is an active field. Papers on AI text detection in non-English languages have been published in ACL, EMNLP, and other NLP venues, examining how well English-trained detection models transfer to other languages and what language-specific approaches are needed. Research on ${lang} specifically covers the unique linguistic features that affect detection reliability.' },
  { category: 'Privacy', question: 'Is my ${lang} text shared with third parties?', answer: 'No — all processing runs in your browser. Your ${lang} text is not transmitted to any server, not shared with third parties, and not used for model training. This is verifiable by monitoring the Network tab in browser developer tools during processing.' },
  { category: 'Use Cases', question: 'Can businesses use the ${toolName} for automated content pipelines?', answer: '${isHumanizer ? `The browser tool is designed for interactive use. For automated ${lang} content pipelines, explore our API or use multilingual NLP libraries (mBERT, XLM-RoBERTa) as a foundation for programmatic humanization workflows.` : `The browser tool is designed for interactive use. For automated screening of ${lang} content at scale, explore our API or use multilingual AI detection libraries that support programmatic batch analysis.`}' },
  { category: 'Workflow', question: 'What is the recommended workflow for ${isHumanizer ? `humanizing` : `detecting AI in`} ${lang} content at scale?', answer: '${isHumanizer ? `For scale: (1) Draft with AI. (2) Humanize in bulk using API access. (3) Human review of flagged sections. (4) Final editorial approval. Humanization accelerates drafting but does not replace editorial review, especially for professional ${lang} content.` : `For scale: (1) Auto-screen submissions with the API. (2) Flag high-confidence AI content for human review. (3) Human reviewer makes final call. (4) Document findings for compliance records. Automated detection is a triage tool, not a replacement for editorial judgment.`}' },
  { category: 'Workflow', question: 'How should I handle ${lang} content that scores in the uncertain middle range?', answer: '${isHumanizer ? `Middle-range AI detection scores on your humanized ${lang} output suggest specific sections are still flagging. Run the text through your target detector, identify the highest-scoring sentences, and manually rewrite those sections. Longer, more context-rich rewriting of flagged sections typically resolves middle-range scores.` : `Middle-range scores (40-60% AI probability) are the most difficult to interpret. Treat these as prompting further investigation rather than definitive findings. Examine which sentences drove the score and whether they are formulaic for the topic, formally written by a non-native ${lang} speaker, or genuinely typical of AI generation.`}' },
  { category: 'Advanced', question: 'How does the ${lang} model handle ${lang} code-switching with English technical terms?', answer: 'Code-switching — mixing ${lang} prose with English technical terms, brand names, or jargon — is common in professional ${lang} writing. The tool handles this naturally: ${isHumanizer ? `English technical terms are left unchanged while the surrounding ${lang} prose is humanized appropriately.` : `English technical terms within ${lang} text are treated as expected code-switching rather than as anomalies, reducing false positives from this common professional writing pattern.`}' },
];

export const ${exportName}: ToolContent = {
  writeUp: <WriteUp />,
  faqs,
};
`;
}

const langFiles = [
  { slug: 'chinese-ai-detector', lang: 'Chinese', isHumanizer: false },
  { slug: 'chinese-ai-humanizer', lang: 'Chinese', isHumanizer: true },
  { slug: 'french-ai-detector', lang: 'French', isHumanizer: false },
  { slug: 'french-ai-humanizer', lang: 'French', isHumanizer: true },
  { slug: 'german-ai-detector', lang: 'German', isHumanizer: false },
  { slug: 'german-ai-humanizer', lang: 'German', isHumanizer: true },
  { slug: 'hindi-ai-detector', lang: 'Hindi', isHumanizer: false },
  { slug: 'hindi-ai-humanizer', lang: 'Hindi', isHumanizer: true },
  { slug: 'indonesian-ai-detector', lang: 'Indonesian', isHumanizer: false },
  { slug: 'indonesian-ai-humanizer', lang: 'Indonesian', isHumanizer: true },
  { slug: 'italian-ai-detector', lang: 'Italian', isHumanizer: false },
  { slug: 'italian-ai-humanizer', lang: 'Italian', isHumanizer: true },
  { slug: 'japanese-ai-detector', lang: 'Japanese', isHumanizer: false },
  { slug: 'japanese-ai-humanizer', lang: 'Japanese', isHumanizer: true },
  { slug: 'korean-ai-detector', lang: 'Korean', isHumanizer: false },
  { slug: 'korean-ai-humanizer', lang: 'Korean', isHumanizer: true },
  { slug: 'portuguese-ai-detector', lang: 'Portuguese', isHumanizer: false },
  { slug: 'portuguese-ai-humanizer', lang: 'Portuguese', isHumanizer: true },
  { slug: 'russian-ai-detector', lang: 'Russian', isHumanizer: false },
  { slug: 'russian-ai-humanizer', lang: 'Russian', isHumanizer: true },
  { slug: 'spanish-ai-detector', lang: 'Spanish', isHumanizer: false },
  { slug: 'spanish-ai-humanizer', lang: 'Spanish', isHumanizer: true },
];

for (const { slug, lang, isHumanizer } of langFiles) {
  const path = resolve(CONTENT_DIR, slug + '.tsx');
  writeFileSync(path, makeLangFile(slug, lang, isHumanizer), 'utf8');
  console.log('Written:', slug);
}

console.log('\nDone — 22 language files regenerated.');

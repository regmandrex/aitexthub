/**
 * Patches gen-content-files.mjs to add more FAQs to each template (reaching 23+ per template).
 */
import { readFileSync, writeFileSync } from 'fs';

let src = readFileSync('scripts/gen-content-files.mjs', 'utf8');

// ── 1. Watermark template: add 9 FAQs after the 'open-source tools' FAQ ─────
const watermarkExtra = `  { category: 'Technical', question: 'What is C2PA and why does it matter for \${aiName} \${mediaType}s?', answer: 'C2PA (Coalition for Content Provenance and Authenticity) is an open standard for cryptographically signed media provenance. A C2PA manifest embedded in a file records who created it, which tool was used, and when — signed with a certificate so the information cannot be tampered with without invalidating the signature. \${aiName} uses C2PA to provide verifiable AI attribution. This tool removes the C2PA manifest, stripping that verifiable attribution layer from the file.' },
  { category: 'Technical', question: 'How does XMP metadata differ from C2PA in \${aiName} \${mediaType}s?', answer: 'XMP (Extensible Metadata Platform) is a flat metadata format used across Adobe tools and many media applications. \${aiName} uses XMP to embed software identification fields. Unlike C2PA, XMP is not cryptographically signed — it can be edited without detection. C2PA provides a tamper-evident signed provenance record. Both are metadata-layer watermarks (as opposed to pixel-level), and both are fully removed by this tool.' },
  { category: 'Privacy', question: 'What information does the \${aiName} watermark reveal about me?', answer: '\${aiName} watermarks typically contain the AI model identifier, a generation timestamp, and a cryptographic hash of the content. Some implementations include API key or account-linked identifiers. Removing these before file delivery ensures that internal workflow details — toolchain, timestamps — are not embedded in deliverable files.' },
  { category: 'Workflow', question: 'Should I remove watermarks from all \${aiName} \${mediaType}s or only some?', answer: 'Best practice: retain watermarks in your internal asset management system where provenance is useful for tracking. \${remover ? "Strip them selectively for deliverables with specific metadata requirements — client delivery, DAM compatibility, technical pipeline requirements." : "Analyze them to verify provenance before including files in your content pipeline."}' },
  { category: 'Workflow', question: 'What should I document when removing \${aiName} watermarks?', answer: 'Document in your asset management system: the original file name and generation timestamp, the AI model version used, the prompt or generation parameters, and the reason for removal. This maintains your internal AI origin record even when the embedded watermark is stripped from the deliverable. For regulatory compliance this documentation may be required by AI disclosure laws applying to commercial content.' },
  { category: 'Comparison', question: 'Is it better to use this tool or just re-upload the \${mediaType} to social media?', answer: 'Social media platforms strip metadata on upload, removing C2PA and XMP watermarks. However, pixel-level watermarks (like SynthID in Google-generated content) survive social media processing because they live in pixel data rather than the metadata layer. This tool removes both layers. For \${mediaType}s where pixel-level signals matter, this tool is significantly more effective than platform upload alone.' },
  { category: 'Advanced', question: 'How do I verify the watermark was successfully removed?', answer: 'For metadata removal: use Adobe content credentials verify to check C2PA, and ExifTool to check XMP fields. A clean file shows no C2PA manifest and no AI-identifying XMP fields. For pixel-level attenuation: upload the processed file to a SynthID detector (for Google-generated content). Reduced confidence scores indicate successful attenuation.' },
  { category: 'Advanced', question: 'Can I process RAW or high-bit-depth \${aiName} \${mediaType} files?', answer: 'The tool supports standard delivery formats: PNG, JPEG, WebP for images; MP4, MOV for video. RAW formats and 16-bit variants are supported for metadata removal but may have limited pixel-level attenuation capability. For professional workflows with high-bit-depth files, use ExifTool for metadata removal in combination with format-appropriate processing tools.' },
  { category: 'Research', question: 'How does \${aiName} watermarking relate to the C2PA open standard?', answer: 'C2PA is an industry-wide open standard that \${aiName} implements alongside its proprietary pixel-level watermarking where applicable. C2PA provides interoperable, verifiable provenance across different AI providers — a DALL-E image and a Firefly image both carry C2PA manifests readable by the same verification tools. Proprietary pixel-level watermarks like SynthID require provider-specific detection tools. \${aiName} balances open standard interoperability with robust pixel-level identification.' },
`;

const watermarkAnchor = `  { category: 'Research', question: 'Are there open-source tools for verifying \${aiName} \${mediaType} watermarks?'`;
if (src.includes(watermarkAnchor)) {
  src = src.replace(watermarkAnchor, watermarkExtra + watermarkAnchor);
  console.log('✓ Watermark FAQs patched');
} else {
  console.log('✗ Watermark anchor not found');
}

// ── 2. Language template: add 7 FAQs after the last FAQ in makeLangFile ─────
const langExtra = `  { category: 'Research', question: 'Is the \${lang} AI \${isHumanizer ? "humanizer" : "detector"} updated as new AI models are released?', answer: '\${isHumanizer ? "The humanizer is updated as major AI models release new versions that introduce new statistical patterns detectors can identify. Keeping the humanizer calibrated to current model outputs ensures humanized text continues to pass updated detection tools." : "The detector is updated as new major AI models are released and as existing models are updated. New model versions may produce output with different statistical signatures, and the detector is retrained to capture these new patterns for " + lang + "."}' },
  { category: 'Research', question: 'Is there academic research on AI \${isHumanizer ? "humanization" : "detection"} in \${lang}?', answer: 'Academic research on multilingual AI \${isHumanizer ? "humanization" : "detection"} is an active field. Papers on AI text detection in non-English languages have been published in ACL, EMNLP, and other NLP venues, examining how well English-trained detection models transfer to other languages and what language-specific approaches are needed. Research on \${lang} specifically covers the unique linguistic features that affect detection reliability.' },
  { category: 'Privacy', question: 'Is my \${lang} text shared with third parties?', answer: 'No — all processing runs in your browser. Your \${lang} text is not transmitted to any server, not shared with third parties, and not used for model training. This is verifiable by monitoring the Network tab in browser developer tools during processing.' },
  { category: 'Use Cases', question: 'Can businesses use the \${lang} \${isHumanizer ? "humanizer" : "detector"} for automated content pipelines?', answer: '\${isHumanizer ? "The browser tool is designed for interactive use. For automated \${lang} content pipelines, explore our API or use multilingual NLP libraries (mBERT, XLM-RoBERTa) as a foundation for programmatic humanization workflows." : "The browser tool is designed for interactive use. For automated screening of \${lang} content at scale, explore our API or use multilingual AI detection libraries that support programmatic batch analysis."}' },
  { category: 'Workflow', question: 'What is the recommended workflow for \${isHumanizer ? "humanizing" : "detecting AI in"} \${lang} content at scale?', answer: '\${isHumanizer ? "For scale: (1) Draft with AI. (2) Humanize in bulk using API access. (3) Human review of flagged sections. (4) Final editorial approval. Humanization accelerates drafting but does not replace editorial review, especially for professional " + lang + " content." : "For scale: (1) Auto-screen submissions with the API. (2) Flag high-confidence AI content for human review. (3) Human reviewer makes final call. (4) Document findings for compliance records. Automated detection is a triage tool, not a replacement for editorial judgment."}' },
  { category: 'Workflow', question: 'How should I handle \${lang} content that scores in the uncertain middle range?', answer: '\${isHumanizer ? "Middle-range AI detection scores on your humanized " + lang + " output suggest specific sections are still flagging. Run the text through your target detector, identify the highest-scoring sentences, and manually rewrite those sections. Longer, more context-rich rewriting of flagged sections typically resolves middle-range scores." : "Middle-range scores (40-60% AI probability) are the most difficult to interpret. Treat these as prompting further investigation rather than definitive findings. Examine which sentences drove the score and whether they are formulaic for the topic, formally written by a non-native " + lang + " speaker, or genuinely typical of AI generation."}' },
  { category: 'Advanced', question: 'How does the \${lang} model handle \${lang} code-switching with English technical terms?', answer: 'Code-switching — mixing \${lang} prose with English technical terms, brand names, or jargon — is common in professional \${lang} writing. The tool handles this naturally: \${isHumanizer ? "English technical terms are left unchanged while the surrounding " + lang + " prose is humanized appropriately." : "English technical terms within " + lang + " text are treated as expected code-switching rather than as anomalies, reducing false positives from this common professional writing pattern."}' },
`;

const langAnchor = `  { category: 'Advanced', question: 'Can I humanize/detect \${lang} text programmatically via API?'`;
if (src.includes(langAnchor)) {
  src = src.replace(langAnchor, langExtra + langAnchor);
  console.log('✓ Language FAQs patched');
} else {
  console.log('✗ Language anchor not found');
}

// ── 3. GPT version template: add 13 FAQs after the last FAQ ─────────────────
const gptExtra = `  { category: 'Use Cases', question: 'What industries benefit most from GPT-\${version} \${isHumanizer ? "humanization" : "detection"}?', answer: '\${isHumanizer ? "Content marketing (blog posts, social media, email), academic assistance (research drafts), creative writing (story starters, scripts), professional communications (reports, proposals), and publishing workflows benefit most from GPT-" + version + " humanization. Any workflow that uses GPT-" + version + " as a drafting tool benefits from humanization before final delivery." : "Academic institutions (integrity screening), publishing (content verification), journalism (source verification), marketing platforms (content quality), legal teams (document provenance), and compliance teams (regulatory AI disclosure) benefit most from GPT-" + version + " detection."}' },
  { category: 'Technical', question: 'What GPT-\${version} output types does the \${isHumanizer ? "humanizer" : "detector"} work best on?', answer: '\${isHumanizer ? "The humanizer works best on general prose: blog posts, essays, email drafts, reports, and creative writing. It is less effective on code, mathematical content, and highly structured technical documents where the formatting requirements constrain natural variation." : "Detection works best on general prose text. Code, mathematical content, and highly structured technical documents have different statistical properties than general prose — the detector is calibrated primarily for natural language text."}' },
  { category: 'Technical', question: 'Does GPT-\${version} use a different tokenizer that affects detection?', answer: 'GPT-\${version} uses OpenAI\\'s tiktoken tokenizer, which affects how the model breaks text into tokens and influences word choice at the margins. The detector accounts for tokenization-related biases in GPT-\${version} output — certain word choices and phrase lengths that are artifacts of the tokenization process and the model\\'s softmax probability distribution over tokens.' },
  { category: 'Comparison', question: 'How does GPT-\${version} compare to Claude and Gemini in terms of AI detection?', answer: '\${isHumanizer ? "The GPT-" + version + " humanizer is specifically calibrated for GPT-" + version + " output patterns. Text from Claude or Gemini has different statistical signatures and may need less adjustment in some areas while needing different adjustments in others. For best results with Claude or Gemini text, use the corresponding Claude or Gemini humanizer." : "GPT-" + version + ", Claude, and Gemini each have distinguishable statistical signatures at the model level. GPT-" + version + " has specific vocabulary distributions, perplexity profiles, and structural patterns. This detector is tuned to GPT-" + version + " specifically and may produce lower accuracy on text from other models."}' },
  { category: 'Comparison', question: 'Is the GPT-\${version} \${isHumanizer ? "humanizer" : "detector"} better than GPTZero or Originality.ai?', answer: '\${isHumanizer ? "This tool is a GPT-" + version + "-specific humanizer designed to counteract the patterns these detectors identify. It\\'s a tool used in conjunction with, not instead of, detectors — use it to make your GPT-" + version + " text score below detection thresholds on tools like GPTZero, Originality.ai, and Turnitin." : "This tool focuses specifically on GPT-" + version + " attribution. GPTZero and Originality.ai are general AI detectors that cover multiple models. For detecting GPT-" + version + " specifically with version-level attribution, this tool provides more specific classification. For general AI detection across all models, general-purpose detectors may be more appropriate."}' },
  { category: 'Research', question: 'Is there published research on detecting GPT-\${version} specifically?', answer: 'Research on AI text detection is published rapidly as new models are released. Papers in ACL, EMNLP, and arXiv cover detection methods for GPT family models including statistical approaches (perplexity, burstiness), classifier-based approaches (fine-tuned BERT/RoBERTa), and retrieval-based approaches. GPT-\${version}-specific research emerges in the months following each model release as researchers characterize the model\\'s output distribution.' },
  { category: 'Research', question: 'How often does the GPT-\${version} \${isHumanizer ? "humanizer" : "detector"} get updated?', answer: 'The tool is updated as OpenAI releases model updates that change GPT-\${version}\\'s output characteristics, and as major AI detection tools update their methods. \${isHumanizer ? "Updates ensure humanized text continues to pass updated detection tools as both the AI model and its detectors evolve." : "Updates ensure the detector stays calibrated to current GPT-" + version + " output rather than becoming stale as the model is updated by OpenAI."}' },
  { category: 'Workflow', question: 'What is the best workflow for using GPT-\${version} in professional content creation?', answer: '\${isHumanizer ? "Best practice: (1) Generate draft with GPT-" + version + ". (2) Run through humanizer. (3) Review output for accuracy and voice. (4) Apply subject matter expertise and fact-checking. (5) Final editorial pass. Treat humanized output as a strong draft requiring expert review, not as publication-ready copy." : "Best practice: (1) Screen submissions or generated content with the detector. (2) Flag high-confidence AI content for human review. (3) Request clarification from authors for borderline cases. (4) Document detection findings for compliance records. Use detection as one component of a multi-method content quality process."}' },
  { category: 'Workflow', question: 'Should I humanize before or after editing GPT-\${version} output?', answer: '\${isHumanizer ? "Humanize first, then edit. Running the humanizer before your editing pass gives you a better starting point for editing — the humanized text is more natural and closer to your final voice target. If you edit first and then humanize, the humanizer may undo some of your manual changes." : "N/A — the detector analyzes text as-is. For the most accurate detection of GPT-" + version + " content, analyze unedited original text. Heavily edited text will show lower AI probability even if the original was fully AI-generated."}' },
  { category: 'Legal', question: 'Are there legal requirements to disclose GPT-\${version}-generated content?', answer: 'AI disclosure requirements vary by jurisdiction and context. The EU AI Act includes requirements for disclosing AI-generated content in certain contexts. FTC guidelines require disclosure of AI-generated reviews and testimonials in the US. Many platforms (Medium, LinkedIn) have their own AI disclosure policies. Using the \${isHumanizer ? "humanizer" : "detector"} does not change your legal obligation to disclose AI-generated content where required by law or platform policy.' },
  { category: 'Legal', question: 'Is it ethical to use a GPT-\${version} humanizer for academic work?', answer: 'Using AI to draft academic work and then humanizing it raises academic integrity concerns that go beyond technical detection. Most academic institutions have policies on AI use that address the ethical question of AI assistance, not just the detection question. Review your institution\\'s current AI policy, as these are being updated rapidly. Many institutions permit AI assistance with disclosure; humanizing to avoid detection without disclosure may violate integrity policies even when technically undetectable.' },
  { category: 'Privacy', question: 'Is my GPT-\${version} text sent to OpenAI when I use this tool?', answer: 'No — this tool is independent of OpenAI and does not transmit text to OpenAI or any other AI provider. All processing runs locally in your browser. Text entered in this tool is not associated with your OpenAI account or used for any model training.' },
  { category: 'Advanced', question: 'Can I test the \${isHumanizer ? "humanizer" : "detector"} on text I know is human-written?', answer: '\${isHumanizer ? "You can input human-written text — the humanizer will make light adjustments if it identifies any patterns that overlap with AI signatures, but will generally leave text that already reads naturally unchanged. This is useful for testing the tool\\'s behavior on your own writing style." : "Yes — testing the detector on known human-written text confirms false positive rate. Human-written text should score below 30% AI probability in most cases. If your own authentic writing consistently scores high, the text may have statistical features that overlap with AI (very formal register, repetitive structure) — or the specific topic area may be underrepresented in the training data."}' },
`;

const gptAnchor = `  { category: 'Advanced', question: 'Can I use this for batch processing multiple GPT-\${version} texts?'`;
if (src.includes(gptAnchor)) {
  src = src.replace(gptAnchor, gptExtra + gptAnchor);
  console.log('✓ GPT version FAQs patched');
} else {
  console.log('✗ GPT anchor not found');
}

// ── 4. Rank tracker template: add 8 FAQs after the last FAQ ─────────────────
const rankExtra = `  { category: 'Strategy', question: 'What types of content help improve \${aiName} rankings?', answer: 'Content that performs best in \${aiName} rankings: comprehensive long-form guides that definitively answer questions in your domain; structured FAQ pages that directly answer common user questions; comparison pages that position your brand against alternatives; case studies and testimonials that provide concrete evidence of your value proposition; and content published in high-authority sources that \${aiName}\\'s training data likely includes. \${aiName} favors comprehensive, accurate, well-structured answers.' },
  { category: 'Strategy', question: 'How does link building affect \${aiName} rankings?', answer: 'Traditional link building (acquiring backlinks to improve Google PageRank) has an indirect effect on \${aiName} rankings. Content that attracts links is typically authoritative and comprehensive — and those same qualities make it more likely to be well-represented in \${aiName}\\'s training data. However, \${aiName} does not evaluate links directly; it responds to the quality and breadth of online coverage about your brand. PR coverage in authoritative publications may have more direct impact than link acquisition campaigns.' },
  { category: 'Strategy', question: 'What role does Wikipedia play in \${aiName} rankings?', answer: 'Wikipedia is significantly overrepresented in AI training data due to its high quality, broad coverage, and permissive licensing. Brands with Wikipedia pages that accurately describe their value proposition have a strong baseline for \${aiName} representation. If your brand meets Wikipedia\\'s notability criteria, maintaining an accurate, well-sourced Wikipedia page is one of the highest-impact GEO strategies. Wikipedia content directly shapes how \${aiName} knows and describes your brand.' },
  { category: 'Strategy', question: 'How do customer reviews affect \${aiName} rankings?', answer: 'Review platforms (G2, Capterra, Trustpilot, Yelp depending on category) are often included in AI training data. A strong presence on these platforms with numerous authentic positive reviews shapes how \${aiName} characterizes your brand when recommending options to users. Review quantity, recency, and sentiment all contribute. Negative reviews are also reflected — reputation management to address legitimate complaints reduces negative signals in AI training data.' },
  { category: 'Metrics', question: 'How do I measure ROI from \${aiName} rank tracking and GEO?', answer: 'Measuring GEO ROI is still an emerging practice. Approaches include: tracking referral traffic from AI-adjacent sources (users who found you in \${aiName} and then visited your site); surveying customers on how they discovered you (AI assistant mentions); monitoring branded search volume as a proxy for AI-driven awareness; and tracking conversion rates from AI-referred customers compared to other channels. As AI assistants gain attribution tools, direct measurement will become easier.' },
  { category: 'Technical', question: 'Does \${aiName} treat different content formats differently in answers?', answer: '\${aiName} is better at extracting information from structured, clearly organized content than from unstructured narrative. Content with clear headings, lists, and direct Q&A formats is more readily incorporated into AI answers. Schema.org markup (FAQ, HowTo, Product schemas) helps structure information in machine-readable formats. For GEO, prioritize clarity and structure over stylistic prose — \${aiName} rewards direct, well-organized information.' },
  { category: 'Technical', question: 'How does \${aiName}\\'s real-time search (if applicable) affect rank tracking?', answer: 'Some \${aiName} deployments have real-time search capability, allowing them to access current web content beyond their training cutoff. When real-time search is active, fresh content and recent reviews can immediately affect your AI ranking — unlike training-data-dependent rankings that only update when the model is retrained. The rank tracker flags whether responses appear to draw on training data or real-time search, which affects your optimization strategy.' },
  { category: 'Comparison', question: 'Should I optimize for \${aiName} rankings or Google featured snippets first?', answer: 'Google featured snippets and \${aiName} rankings respond to similar optimization signals: comprehensive content, clear structure, direct answers to questions. In practice, content optimized for Google featured snippets often performs well in \${aiName} rankings too. For most businesses, optimizing for both simultaneously is efficient — the content investments overlap significantly. \${aiName}-specific enhancements (Wikipedia presence, structured knowledge, authority-building PR) are additive to traditional SEO rather than competing with it.' },
`;

const rankAnchor = `  { category: 'Advanced', question: 'How does \${aiName}\\'s training data update affect my rankings?'`;
if (src.includes(rankAnchor)) {
  src = src.replace(rankAnchor, rankExtra + rankAnchor);
  console.log('✓ Rank tracker FAQs patched');
} else {
  console.log('✗ Rank tracker anchor not found');
}

// ── 5. Humanizer template: add 10 FAQs after the last FAQ ────────────────────
// Use string concatenation to avoid ${} interpolation issues with backtick template
const humanizerExtra = [
  '  { category: \'Quality\', question: \'What makes the ${title} different from basic paraphrasing tools?\', answer: \'Basic paraphrasing tools substitute synonyms and shuffle sentence order — they do not address the underlying statistical patterns that AI detectors identify. The ${title} analyzes and rewrites the statistical signature of the text: varying sentence complexity distribution, adjusting vocabulary toward more idiomatic usage, introducing the natural discourse structure of human writers, and removing the formulaic transitions that AI models consistently produce. The result passes AI detection tools because it addresses the actual detection signals, not just surface wording.\' },',
  '  { category: \'Quality\', question: \'Does the ${title} preserve technical accuracy and specific details?\', answer: \'The tool is designed to preserve semantic content while transforming stylistic and statistical properties. Factual claims, technical details, and specific data points are preserved. However, always review output before use — occasional rephrasing may subtly shift emphasis or meaning in ways that require correction. For content with high-stakes accuracy requirements (medical, legal, financial), treat output as a draft requiring expert review.\' },',
  '  { category: \'Platform\', question: \'Does the ${title} help with platform-specific AI detection systems?\', answer: \'Major platforms (Turnitin, Originality.ai, GPTZero, Copyleaks, Sapling) each use different detection methods. The humanizer targets the common underlying statistical patterns that most detectors look for, rather than gaming any specific detector. This makes the humanization more durable across the broad ecosystem of detection tools rather than just passing one specific system.\' },',
  '  { category: \'Platform\', question: \'Is AI-humanized content compliant with platform terms of service?\', answer: \'Platform policies on AI content vary widely and are evolving rapidly. Some platforms require disclosure of AI assistance; others prohibit AI-generated content in certain contexts (academic submissions, job applications); others are silent on the issue. Review the specific terms of service of the platform you publish on. The ${title} is a writing tool — the compliance and disclosure obligations depend on how and where you use the output.\' },',
  '  { category: \'Use Cases\', question: \'Can the ${title} help non-native English speakers?\', answer: \'Yes — non-native English writers using AI as a drafting assistant can use the humanizer to produce more naturally-sounding output that reflects authentic English usage patterns rather than the slightly formal, non-idiomatic patterns common in both AI writing and non-native writing. The humanizer introduces the idiomatic expressions, contractions, and discourse patterns that native English speakers naturally use.\' },',
  '  { category: \'Use Cases\', question: \'Is the ${title} suitable for long-form content like books or course materials?\', answer: \'Yes, with some considerations. The tool handles inputs up to approximately 3,000 words — for longer content, process it in sections and review section boundaries for consistency. Long-form content humanized in sections may show slight variation in style between sections that requires manual harmonization. The tool is most efficient as a chapter-by-chapter or section-by-section workflow accelerator for long-form content.\' },',
  '  { category: \'Ethics\', question: \'What are the ethical considerations of using the ${title}?\', answer: \'Ethical use involves using the tool for legitimate writing assistance while being transparent about AI involvement where that information is material to your audience. Using the tool to produce high-quality draft content that you meaningfully review, edit, and take responsibility for is defensible. Using it to submit AI-generated content as your own work in contexts where that is explicitly prohibited — academic submissions, platform terms — is an ethical and potentially policy violation regardless of detection outcomes.\' },',
  '  { category: \'Ethics\', question: \'Should I disclose AI involvement when using the ${title}?\', answer: \'Disclosure requirements depend on context. Academic contexts: check your institution policy — most now require disclosure of AI assistance. Professional publishing: follow platform guidelines, many of which now require AI disclosure. Commercial content: FTC guidelines require disclosure in contexts where AI-generated content could mislead consumers (reviews, testimonials). In most other contexts, disclosure is ethically advisable though not legally required. Treat the humanizer as making AI-assisted writing more natural, not as eliminating the need for disclosure where disclosure is appropriate.\' },',
  '  { category: \'Advanced\', question: \'Can I fine-tune the ${title} to match a specific writing style or brand voice?\', answer: \'The current tool applies general humanization without style-specific tuning. For brand-voice-specific humanization, the best approach is to use the tool for base humanization and then edit the output to match your specific brand voice and style guide. For teams that consistently work with a particular voice, building a style guide review into the post-humanization workflow produces more consistent brand-aligned output.\' },',
  '  { category: \'Advanced\', question: \'Does humanized content retain SEO value?\', answer: \'Yes — SEO value depends on content quality, keyword relevance, and helpfulness rather than on whether the text was AI-generated or humanized. Humanized content that is comprehensive, well-structured, and genuinely useful ranks well. The humanization process does not remove keywords or alter the informational structure of the content. In fact, humanized content may perform better in search than raw AI output because it reads as higher-quality to both human users and search engine quality evaluators.\' },',
].join('\n') + '\n';

const humanizerAnchor = `  { category: 'Research', question: 'Is AI-humanized content penalized by search engines or platforms?'`;
if (src.includes(humanizerAnchor)) {
  src = src.replace(humanizerAnchor, humanizerExtra + humanizerAnchor);
  console.log('✓ Humanizer FAQs patched');
} else {
  console.log('✗ Humanizer anchor not found');
}

writeFileSync('scripts/gen-content-files.mjs', src, 'utf8');
console.log('\nDone patching gen-content-files.mjs');
